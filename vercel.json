{
  "buildCommand": "prisma generate && node scripts/generate-sitemap.js && vite build && tsc -p tsconfig.server.json",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api?path=$1"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}