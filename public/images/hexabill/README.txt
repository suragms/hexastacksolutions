Expected screenshots (after `npm run sync:images` from project `images/hexabill/`):
  POS.png
  erp.png
  pos-dashboard.png

SVG fallbacks (hexabill-*.svg) remain if PNGs are removed.

Vite only serves files under public/.
